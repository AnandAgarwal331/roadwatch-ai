# URL the Next.js server uses to reach the FastAPI backend.
# Only the Next.js server reads this - the browser never calls the API
# directly, it goes through /api/proxy/*.
BACKEND_INTERNAL_URL=http://localhost:8000
